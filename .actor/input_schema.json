{
  "title": "On-Chain Attestation",
  "description": "Create cryptographic proofs of your data on Solana blockchain. The data is hashed with SHA-256 and the hash is written on-chain as an immutable, timestamped record.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "solana_private_key": {
      "title": "Solana Private Key",
      "type": "string",
      "description": "Your Solana wallet private key (base58 encoded). The wallet needs SOL for transaction fees (~0.000005 SOL per attestation).",
      "editor": "textfield",
      "isSecret": true,
      "sectionCaption": "Wallet Configuration",
      "sectionDescription": "Provide your Solana wallet credentials. Used to sign and pay for attestation transactions. Your keys are never stored or logged."
    },
    "solana_rpc_url": {
      "title": "Solana RPC URL (Optional)",
      "type": "string",
      "description": "Custom Solana RPC endpoint. Leave empty to use the default public endpoint. Use a private RPC for better reliability.",
      "editor": "textfield",
      "sectionCaption": "Advanced Settings",
      "sectionDescription": "Optional settings for power users. Leave empty to use defaults."
    },
    "data": {
      "title": "Data to Attest",
      "type": "object",
      "description": "The JSON data you want to create a proof for. Only the SHA-256 hash goes on-chain (not the full data). You can attest any JSON structure.",
      "editor": "json",
      "prefill": {
        "product": "Example Product",
        "price": 49.99,
        "scraped_at": "2026-03-06T14:30:00Z",
        "source": "https://example.com/product/123"
      },
      "sectionCaption": "Data",
      "sectionDescription": "The data to create a proof for. For webhook mode, this is auto-populated from the source Actor run."
    },
    "private_mode": {
      "title": "Private attestation",
      "type": "boolean",
      "description": "When enabled, only the hash goes on-chain. Origin, target, and metadata stay private in Apify. Share the attestation ID with buyers/auditors to let them verify.",
      "default": true,
      "sectionCaption": "Privacy Options",
      "sectionDescription": "Control what goes on-chain vs stays private."
    },
    "store_full_data": {
      "title": "Store full data in Apify",
      "type": "boolean",
      "description": "Save the complete data in Apify's Key-Value store alongside the on-chain hash. Required for later verification.",
      "default": true
    },
    "verify_mode": {
      "title": "Verification mode",
      "type": "boolean",
      "description": "Enable this to verify an existing attestation instead of creating a new one. You'll need to provide the transaction hash below.",
      "default": false,
      "sectionCaption": "Verification Mode",
      "sectionDescription": "Use this section to verify an existing attestation by comparing the on-chain hash with your data."
    },
    "tx_hash": {
      "title": "Transaction Hash to Verify",
      "type": "string",
      "description": "The Solana transaction signature of the attestation you want to verify. Required when verification mode is enabled.",
      "editor": "textfield"
    }
  },
  "required": ["solana_private_key"]
}
